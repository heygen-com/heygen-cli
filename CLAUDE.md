# HeyGen CLI

@AGENTS.md

## Pre-Release

Run `/e2e-cli-test` before cutting a stable release. It builds `./bin/heygen` and exercises it against the live API (auth, list, get, --human, schema, error handling, create-poll-download-delete). Requires `HEYGEN_API_KEY`. See RELEASE.md for the full checklist.
